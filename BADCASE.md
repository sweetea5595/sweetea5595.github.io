# 大纲助手 Bad Case 记录

> 每个bad case记录：问题描述 → 复现步骤 → 根因分析 → 修复状态

---

## BC-001 | 地图v3仪表盘 — 地图数据格式不一致导致空状态误判

**发现日期**：2026-06-07  
**严重程度**：中  
**状态**：未修复  

**问题描述**：  
想法梳理AI回复提取出的map数据格式是 `{threads, events, connections}`（nodes格式），但地图v3仪表盘期望的是 `{summary: {threads}, events, contradictions, missing}`（仪表盘格式）。虽然有 `convertFromNodes()` 做转换，但转换后 `contradictions` 和 `missing` 永远为空数组，用户在想法梳理里看到的矛盾点和缺失项不会出现在地图侧边栏。

**复现步骤**：  
1. 在想法梳理里输入一个故事想法
2. 等AI回复（包含矛盾点和缺失项）
3. 自动提取到地图后，查看仪表盘左侧
4. 矛盾点和缺失项区域为空

**根因分析**：  
`extractMapData()` 只提取 threads/events/connections，不提取 contradictions/missing。`convertFromNodes()` 也没有做这部分转换。

---

## BC-002 | 地图v3仪表盘 — 事件没有desc描述字段

**发现日期**：2026-06-07  
**严重程度**：低  
**状态**：未修复  

**问题描述**：  
从对话中自动提取的事件节点只有 `id`, `label`, `thread`, `strength` 字段，没有 `desc`（描述）。仪表盘时间线里每个事件的描述区域为空，只显示一个标签名。

**复现步骤**：  
1. 在想法梳理里输入故事想法
2. AI回复后自动提取到地图
3. 查看右侧事件时间线，事件下方无描述文字

**根因分析**：  
`extractMapData()` 的事件提取逻辑只取 label（截断到8字），不取描述。AI回复中的事件描述没有被解析。

---

## BC-003 | 想法梳理 — Markdown渲染列表嵌套问题

**发现日期**：2026-06-07  
**严重程度**：低  
**状态**：未修复  

**问题描述**：  
`renderMarkdown()` 的列表处理比较简单，连续的 `<li>` 会被包进一个 `<ul>`，但如果AI回复中有嵌套列表或带编号的列表，渲染结果会错乱。

**复现步骤**：  
1. 让AI生成包含嵌套列表或编号列表的回复
2. 观察渲染结果

---

## BC-004 | 地图v3仪表盘 — 样例数据硬编码，新用户看到的不是自己的故事

**发现日期**：2026-06-07  
**严重程度**：中  
**状态**：未修复  

**问题描述**：  
`getMapData()` 在 localStorage 没数据时返回 `SAMPLE_DATA`（真千金逆袭）。新用户第一次打开地图看到的是别人的故事，容易困惑。

**复现步骤**：  
1. 清除 localStorage
2. 直接点击"全貌地图"气泡
3. 看到"真千金回归后逆袭翻盘"的样例数据

**建议**：  
应该显示空状态引导（"去想法梳理聊聊你的故事"），而不是硬编码的样例数据。

---

## BC-005 | 灵感记录 — 示例灵感点击后变成真实灵感，无法区分

**发现日期**：2026-06-07  
**严重程度**：低  
**状态**：未修复  

**问题描述**：  
`SAMPLE_INSPIRATIONS` 有 `sample: true` 标记，但点击示例灵感调用 `addInspirationDirect()` 后，新添加的灵感没有保留 `sample` 标记，用户无法区分哪些是自己加的、哪些是示例。

---

## BC-006 | 想法梳理 — 对话历史不持久化

**发现日期**：2026-06-07  
**严重程度**：中  
**状态**：未修复  

**问题描述**：  
`ideaConversation` 数组只存在内存中，刷新页面后对话历史丢失。灵感数据用了 localStorage 持久化，但聊天记录没有。

**复现步骤**：  
1. 在想法梳理里跟AI聊几轮
2. 刷新页面
3. 对话记录全部消失

---

## BC-007 | 想法梳理 — 打字机效果中滚动不够平滑

**发现日期**：2026-06-07  
**严重程度**：低  
**状态**：未修复  

**问题描述**：  
`typeAIMessage()` 中每段出现后直接 `scrollTop = scrollHeight`，没有平滑滚动效果，视觉上跳跃感明显。当AI回复很长时，用户体验不佳。

---

---

## BC-008 | 全貌地图 — 内容多时无法滚动

**发现日期**：2026-06-07
**严重程度**：高
**状态**：✅ 已修复（v3.5.1）

**问题描述**：
地图面板内容多时（事件多、故事线多），底部内容被截断看不到。`.map-canvas` 没有设置 `overflow-y: auto`，`.dashboard` 的 `overflow: hidden` 也阻止了内部滚动。

**修复**：
1. `.map-canvas` 添加 `flex: 1; min-height: 0; overflow-y: auto;`
2. `.dashboard` 的 `overflow: hidden` 改为 `overflow-y: auto`

---

<!-- 新增bad case在上面添加，格式：BC-XXX | 模块 — 问题标题 -->
